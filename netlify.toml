[build]
  command = "pnpm build"
  publish = "dist"

# Redirect slashless blog URLs to their trailing-slash versions
[[redirects]]
  from = "/blog/:slug"
  to = "/blog/:slug/"
  status = 301
  force = true